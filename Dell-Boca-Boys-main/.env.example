# Dell Bocca Boys Configuration

# Email Communication Settings
# Official agent email: ace.llc.nyc@gmail.com
AGENT_EMAIL="ace.llc.nyc@gmail.com"
AGENT_EMAIL_PASSWORD="your-16-char-gmail-app-password-here"
EMAIL_POLL_INTERVAL="60"
EMAIL_SERVICE_ENABLED="true"

# Agent Configuration
# Add other agent-specific configuration here

# -----------------------------------------------------------------------------
# LLM Configuration
# These settings control the local and remote language models used by the agents.
#
# OLLAMA_BASE_URL:   Base URL for your local Ollama server (default: http://localhost:11434)
# LOCAL_MODEL_NAME:  Name of the local model to load via Ollama (e.g. qwen2.5-coder:latest)
# GEMINI_API_KEY:    API key for Google Gemini; leave blank to disable Gemini
# GEMINI_MODEL:      Gemini model variant (e.g. gemini-1.5-pro-latest)
# LLM_FALLBACK:      Behaviour when local model fails (options: 'gemini' or 'none')

OLLAMA_BASE_URL="http://localhost:11434"
LOCAL_MODEL_NAME="qwen2.5-coder:latest"
GEMINI_API_KEY=""
GEMINI_MODEL="gemini-1.5-pro-latest"
LLM_FALLBACK="gemini"

# GLM model configuration (for the legacy web dashboard)
# LOCAL_GLM_MODEL_PATH: absolute or relative path to the GLM model directory.
# If unset, the application will fall back to the `glm4.5-air-mlx-4bit` folder
# inside the project root.  This setting is used by the dashboard API.
LOCAL_GLM_MODEL_PATH=""
