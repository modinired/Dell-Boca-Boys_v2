# SECURITY & PRIVACY

- Keep client data local and encrypted at rest.
- Remove PII wherever possible (use IDs over names where feasible).
- Version-control code, **not** raw client data.
- For email exports, password-protect archives.
